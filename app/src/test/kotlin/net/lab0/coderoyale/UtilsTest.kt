package net.lab0.coderoyale

import MapSite
import org.assertj.core.api.Assertions.assertThat
import org.junit.jupiter.api.Test

internal class UtilsTest {
  companion object {
    val mapSites = listOf(MapSite(0, Position.ORIGIN, 0))
  }

}